{{#link-to 'item' product.id}}
  <div class="well shoeCard">
    <div class="shoeTitleText">
      <h3>{{product.title}}</h3>
    </div>
    <img class="shoePic smallImage" src={{product.image}}>
  </div>
{{/link-to}}

<!-- <button class="btn btn-primary" {{action 'addToCart' product}}>Add to Cart</button> -->
