import { reviewRatingStars } from 'online-marketplace/helpers/review-rating-stars';
import { module, test } from 'qunit';

module('Unit | Helper | review rating stars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviewRatingStars([42]);
  assert.ok(result);
});
